const nav=document.querySelector('nav');document.querySelector('.nav-toggle').addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(link=>link.addEventListener('click',()=>nav.classList.remove('open')));document.querySelectorAll('.product-grid button').forEach(button=>button.addEventListener('click',()=>{button.textContent='Added to enquiry';button.disabled=true}));
